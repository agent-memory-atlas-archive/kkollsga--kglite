---
title: Notes index
---
In a vault `index.md` is an ordinary note. The `okf` and `loose` dialects
divert it to their folder's metadata instead; this dialect does not.

The handbook ![Handbook](img/handbook.pdf) is an `Attachment`, resolved
vault-root-relative.
