---
title: Projects
---
The folder note for `projects/`. It takes the directory's place: there is no
`Folder` node for `projects`, and every note inside is joined to this one by
`CHILD_OF` instead of being `CONTAINS`ed by a folder.
